# 数据源配置指南

## 问题说明

在使用 DataManager 下载数据时，如果出现错误：
```
Failed to query bar data: the datafeed is not configured correctly.
```

这表示数据源（datafeed）没有正确配置。

## 解决方案

### 方法1：通过GUI界面配置（推荐）

1. 启动 VeighNa Trader
2. 点击菜单栏的【配置】按钮
3. 在弹出的全局配置对话框中，找到数据服务相关配置：
   - **datafeed.name**: 数据服务名称（如 `rqdata`、`xt` 等）
   - **datafeed.username**: 数据服务的用户名
   - **datafeed.password**: 数据服务的密码或license

4. 填写配置信息后，点击【确定】保存

### 方法2：使用配置脚本（推荐，最简单）

运行配置脚本：

```bash
python configure_tushare.py
```

然后输入你的TuShare Token即可自动配置。

### 方法3：通过代码配置

在启动脚本中（如 `run_with_accounts.py`）添加以下代码：

**配置TuShare（推荐）**：
```python
from vnpy.trader.setting import SETTINGS

# 配置TuShare数据服务
SETTINGS["datafeed.name"] = "tushare"
SETTINGS["datafeed.username"] = "token"  # TuShare固定为"token"
SETTINGS["datafeed.password"] = "你的TuShare Token"  # 替换为你的实际Token
```

**配置RQData**：
```python
from vnpy.trader.setting import SETTINGS

# 配置RQData数据服务
SETTINGS["datafeed.name"] = "rqdata"
SETTINGS["datafeed.username"] = "license"  # RQData用户名统一为"license"
SETTINGS["datafeed.password"] = "你的RQData License"  # 替换为你的实际license
```

## 支持的数据服务

### TuShare（推荐，性价比高）✅ 已安装

- **模块名称**: `vnpy_tushare` ✅ 已安装
- **数据分类**: 股票、期货、指数、基金等
- **数据周期**: 日线、分钟线（根据Token等级）
- **价格**: 免费版有积分限制，付费版价格相对较低
- **配置示例**:
  ```python
  SETTINGS["datafeed.name"] = "tushare"
  SETTINGS["datafeed.username"] = "token"  # TuShare固定为"token"
  SETTINGS["datafeed.password"] = "你的TuShare Token"  # 替换为你的实际Token
  ```
- **获取Token**:
  1. 访问 [Tushare Pro](https://tushare.pro/)
  2. 注册账号并登录
  3. 在个人中心 -> 接口Token 处获取你的Token
  4. 免费版有积分限制，付费版功能更强大
- **注意**: 
  - username 固定为 "token"
  - password 是你从Tushare Pro获取的Token（不是登录密码）
  - 免费版每日有积分限制，建议注册付费版获得更好的体验

### RQData（已安装）

- **模块名称**: `vnpy_rqdata` ✅ 已安装
- **配置示例**:
  ```python
  SETTINGS["datafeed.name"] = "rqdata"
  SETTINGS["datafeed.username"] = "license"
  SETTINGS["datafeed.password"] = "你的RQData License"
  ```
- **注册申请**: [RICEQUANT](https://www.ricequant.com/welcome/purchase?utm_source=vnpy)
- **注意**: username 统一为 "license"，password 是你购买的 RQData License

### 其他数据服务

如果需要使用其他数据服务，需要先安装对应的模块：

- **迅投研 (XT)**: `pip install vnpy_xt`
- **UData**: `pip install vnpy_udata`
- **TQSDK**: `pip install vnpy_tqsdk`
- **Wind**: `pip install vnpy_wind`
- **iFinD**: `pip install vnpy_ifind`
- **Tinysoft**: `pip install vnpy_tinysoft`

## 配置验证

配置完成后，可以通过以下方式验证：

1. 在 DataManager 中尝试下载数据
2. 如果配置正确，应该能够成功下载数据
3. 如果仍有错误，请检查：
   - 数据服务模块是否已安装
   - 用户名和密码/License 是否正确
   - 网络连接是否正常

## 注意事项

1. **RQData License**: 
   - 需要购买或申请试用 RQData License
   - License 不是米筐官网的登录账号密码
   - 获取方式：访问 [RICEQUANT](https://www.ricequant.com/welcome/purchase?utm_source=vnpy)

2. **配置文件位置**:
   - 全局配置保存在 `.vntrader/vt_setting.json` 文件中
   - 修改后会自动保存

3. **数据服务选择**:
   - 根据你的需求选择合适的数据服务
   - 不同数据服务提供的数据范围和价格不同

## 快速测试

配置完成后，可以在 DataManager 中测试下载：

### TuShare测试数据

1. 打开【功能】->【数据管理】
2. 点击【下载数据】
3. 填写测试数据（股票）：
   - 代码: `000001`（平安银行）
   - 交易所: `SZSE`（深交所）
   - 周期: `DAILY`
   - 开始日期: `2024/01/01`
4. 或填写测试数据（期货）：
   - 代码: `IF888`（股指期货）
   - 交易所: `CFFEX`（中金所）
   - 周期: `DAILY`
   - 开始日期: `2024/01/01`
5. 点击【下载】按钮
6. 如果配置正确，应该能够成功下载数据

### 常见问题排查

- **Token错误**：检查Token是否正确，是否有空格
- **积分不足**：登录 https://tushare.pro/ 查看剩余积分
- **数据不存在**：确认合约代码和交易所是否正确

