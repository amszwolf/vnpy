# 设置 dev 分支为默认分支

## 方法1：通过GitHub网页设置（推荐）

1. 访问仓库：https://github.com/amszwolf/vnpy
2. 点击仓库右上角的 **Settings**（设置）
3. 在左侧菜单中找到 **Branches**（分支）
4. 在 **Default branch**（默认分支）部分，点击分支名称旁边的切换图标
5. 选择 **dev** 分支
6. 点击 **Update**（更新）
7. 确认更改

## 方法2：使用GitHub API（需要Personal Access Token）

如果需要通过命令行设置，可以使用以下命令：

```powershell
# 需要先设置GitHub Personal Access Token
$token = "your_github_token_here"
$repo = "amszwolf/vnpy"

# 使用GitHub API设置默认分支
$headers = @{
    "Authorization" = "token $token"
    "Accept" = "application/vnd.github.v3+json"
}

$body = @{
    default_branch = "dev"
} | ConvertTo-Json

Invoke-RestMethod -Uri "https://api.github.com/repos/$repo" -Method PATCH -Headers $headers -Body $body -ContentType "application/json"
```

## 当前状态

✅ dev分支已创建并推送
✅ 所有代码已提交到dev分支
⏳ 等待设置dev为默认分支

## 提交信息

- **分支**: dev
- **提交ID**: a801204c
- **文件数**: 22个文件
- **新增行数**: 7022行
- **修改内容**:
  - 添加自定义数据路径配置
  - 添加CTP账号管理功能
  - 增强数据录制功能
  - 修复回测组合功能
  - 添加多个辅助脚本和文档

