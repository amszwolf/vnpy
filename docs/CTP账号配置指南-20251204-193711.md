# CTP Gateway 账号配置指南

根据 `gateway.md` 文档，CTP接口的完整配置说明。

## 一、CTP接口信息

### 接口支持
- **操作系统**: Windows, Ubuntu
- **交易品种**: 期货、期货期权
- **持仓方向**: 只支持双向持仓
- **历史数据**: 不提供

### 配置字段（必填）

| 字段名 | 说明 | 示例 |
|--------|------|------|
| 用户名 | 交易账号（6位纯数字） | 123456 |
| 密码 | 交易密码 | your_password |
| 经纪商代码 | 期货公司代码（4位数字） | 9999（SimNow） |
| 交易服务器 | 交易服务器地址 | tcp://180.168.146.187:10130 |
| 行情服务器 | 行情服务器地址 | tcp://180.168.146.187:10131 |
| 产品名称 | 产品信息（可选） | "" |
| 授权编码 | 授权码（可选） | "" |

## 二、获取账号

### 仿真账号（SimNow）
1. 访问 [SimNow网站](http://www.simnow.com.cn/)
2. 输入手机号码和短信验证码注册
3. 用户名（InvestorID）为6位纯数字
4. 经纪商编号为 **9999**
5. 提供两套环境：
   - 盘中仿真交易环境
   - 盘后测试环境
6. **注意**：需要修改一次密码之后才能使用
7. **注意**：每套仿真环境的适用时间段不同

### 实盘账号
1. 在期货公司开户
2. 联系客户经理开通CTP接口
3. 用户名为纯数字
4. 经纪商编号为4位纯数字（每个期货公司不同）
5. 实盘账号也可以开通仿真交易功能

## 三、配置方法

### 方法1：使用工具脚本（推荐）

编辑 `add_accounts.py`，填入账号信息：

```python
accounts = [
    {
        "account_name": "账号1",  # 多账号时用于区分
        "gateway_type": "CTP",
        "username": "123456",     # 您的用户名
        "password": "your_password",  # 您的密码
        "broker_id": "9999",     # SimNow为9999，实盘为期货公司代码
        "md_address": "tcp://180.168.146.187:10131",  # 行情服务器
        "td_address": "tcp://180.168.146.187:10130",  # 交易服务器
        "product_name": "",      # 可选
        "auth_code": ""          # 可选
    }
]
```

运行脚本：
```powershell
conda activate vnpyenv
python add_accounts.py
```

### 方法2：直接编辑JSON文件

配置文件位置：`C:\Users\YourName\.vntrader\connect_ctp.json`

编辑内容：
```json
{
    "用户名": "123456",
    "密码": "your_password",
    "经纪商代码": "9999",
    "交易服务器": "tcp://180.168.146.187:10130",
    "行情服务器": "tcp://180.168.146.187:10131",
    "产品名称": "",
    "授权编码": ""
}
```

### 方法3：通过GUI界面

1. 启动VeighNa Trader
2. 点击【系统】->【连接CTP】
3. 在弹出的对话框中填入账号信息
4. 点击【连接】按钮
5. 配置会自动保存

## 四、运行CTP Gateway

### 安装CTP Gateway模块

```powershell
conda activate vnpyenv
pip install vnpy_ctp
```

### 运行启动脚本

```powershell
conda activate vnpyenv
python run_ctp.py
```

脚本会自动：
- ✅ 检测CTP Gateway模块
- ✅ 加载所有CTP账号配置
- ✅ 注册CTP Gateway
- ✅ 自动连接所有配置的账号
- ✅ 启动VeighNa Trader界面

## 五、多个账号配置

如果有多个CTP账号，可以为每个账号创建独立的配置文件：

- `connect_ctp.json` - 默认账号
- `connect_ctp_账号1.json` - 账号1
- `connect_ctp_账号2.json` - 账号2

脚本会自动检测并加载所有配置文件，为每个账号创建独立的Gateway实例。

## 六、服务器地址

### SimNow仿真环境

**盘中环境（交易日 8:00-16:00）**：
- 交易服务器：`tcp://180.168.146.187:10130`
- 行情服务器：`tcp://180.168.146.187:10131`

**盘后环境（交易日 16:00-次日 8:00）**：
- 交易服务器：`tcp://180.168.146.187:10201`
- 行情服务器：`tcp://180.168.146.187:10211`

### 实盘环境

实盘环境的服务器地址由期货公司提供，请联系客户经理获取。

## 七、验证配置

运行测试脚本查看已配置的账号：

```powershell
conda activate vnpyenv
python test_account_config.py
```

## 八、常见问题

### 问题1：连接失败
- 检查用户名和密码是否正确
- 检查服务器地址是否正确
- 检查网络连接
- 查看VeighNa Trader日志窗口获取详细错误信息

### 问题2：提示"vnpy_ctp未安装"
```powershell
pip install vnpy_ctp
```

### 问题3：SimNow账号无法登录
- 确保已修改初始密码
- 检查是否在正确的交易时间段
- 确认使用的是正确的服务器地址（盘中/盘后）

### 问题4：实盘账号无法连接
- 确认已开通CTP接口权限
- 检查穿透式认证是否通过
- 联系期货公司客户经理确认

## 九、配置文件位置

配置文件存储在 `.vntrader` 文件夹中：
- 优先：当前工作目录下的 `.vntrader` 文件夹
- 备选：用户主目录下的 `.vntrader` 文件夹

查看配置文件位置：
```python
from vnpy.trader.utility import TEMP_DIR
print(f"配置文件位置: {TEMP_DIR}")
```

## 十、完整示例

### 示例1：SimNow仿真账号

```json
{
    "用户名": "123456",
    "密码": "your_password",
    "经纪商代码": "9999",
    "交易服务器": "tcp://180.168.146.187:10130",
    "行情服务器": "tcp://180.168.146.187:10131",
    "产品名称": "",
    "授权编码": ""
}
```

### 示例2：实盘账号

```json
{
    "用户名": "789012",
    "密码": "your_password",
    "经纪商代码": "0123",
    "交易服务器": "tcp://实盘服务器IP:端口",
    "行情服务器": "tcp://实盘服务器IP:端口",
    "产品名称": "您的产品名称",
    "授权编码": "您的授权编码"
}
```

## 十一、下一步

1. ✅ 安装CTP Gateway模块：`pip install vnpy_ctp`
2. ✅ 配置账号信息：编辑 `add_accounts.py` 或直接编辑JSON文件
3. ✅ 运行启动脚本：`python run_ctp.py`
4. ✅ 查看连接状态：在VeighNa Trader的日志窗口中查看

