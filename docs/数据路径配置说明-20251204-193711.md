# VeighNa 数据路径配置说明

## 当前配置

数据存储路径已从默认位置修改为：
- **新路径**: `D:\Data\vnpy`
- **原路径**: `C:\Users\HailongHan_ot5a18w\.vntrader`

## 修改方法

在 `examples/data_recorder/data_recorder.py` 文件中，已添加了路径配置代码（第12-36行）：

```python
# ============================================================
# 设置自定义数据存储路径
# ============================================================
CUSTOM_DATA_DIR = Path(r"D:\Data\vnpy")
if not CUSTOM_DATA_DIR.exists():
    CUSTOM_DATA_DIR.mkdir(parents=True, exist_ok=True)
    print(f"✓ 创建数据目录: {CUSTOM_DATA_DIR}")

# 加载VeighNa核心框架
from vnpy.event import EventEngine, Event
# ... 其他导入 ...

# 修改VeighNa的数据目录路径
# 必须在数据库初始化之前修改
import vnpy.trader.utility as trader_utility
trader_utility.TRADER_DIR = CUSTOM_DATA_DIR
trader_utility.TEMP_DIR = CUSTOM_DATA_DIR
```

## 存储的数据

以下数据会保存到 `D:\Data\vnpy` 目录：

1. **数据库文件**
   - `database.db` - SQLite数据库（存储Tick和K线数据）

2. **配置文件**
   - `connect_ctp.json` - CTP连接配置
   - `data_recorder_setting.json` - 数据录制模块配置
   - `vt_setting.json` - VeighNa全局设置
   - 其他配置文件

3. **日志文件**
   - `log/` - 日志文件目录

4. **其他数据**
   - `ctp/` - CTP相关文件
   - 其他模块生成的文件

## 在其他脚本中使用

如果需要在其他脚本中也使用自定义路径，可以：

1. **方法1：复制路径设置代码**
   ```python
   from pathlib import Path
   import vnpy.trader.utility as trader_utility
   
   CUSTOM_DATA_DIR = Path(r"D:\Data\vnpy")
   trader_utility.TRADER_DIR = CUSTOM_DATA_DIR
   trader_utility.TEMP_DIR = CUSTOM_DATA_DIR
   ```

2. **方法2：使用通用配置模块**
   ```python
   from vnpy_custom_path import set_custom_data_path
   set_custom_data_path(r"D:\Data\vnpy")
   ```

## 验证路径

运行以下代码可以验证路径是否正确：

```python
from vnpy.trader.utility import TEMP_DIR
print(f"数据存储路径: {TEMP_DIR}")
```

## 注意事项

1. 路径修改必须在导入 `vnpy.trader` 相关模块之后、数据库初始化之前进行
2. 如果目录不存在，程序会自动创建
3. 修改路径后，原有的数据仍在原路径，需要手动迁移（如果需要）

