# 快速开始 - 自动加载账号

## 一、已创建的文件

1. **`run_with_accounts.py`** - 自动加载账号的启动脚本
2. **`add_accounts.py`** - 批量添加账号的工具
3. **`test_account_config.py`** - 测试配置加载功能

## 二、使用步骤

### 步骤1：添加您的真实账号信息

**方法A：使用工具脚本（推荐）**

1. 编辑 `add_accounts.py` 文件
2. 找到 `accounts` 列表，填入您的账号信息：

```python
accounts = [
    {
        "account_name": "账号1",
        "gateway_type": "CTP",
        "username": "您的真实用户名",
        "password": "您的真实密码",
        "broker_id": "您的经纪商代码",
        "md_address": "tcp://行情服务器IP:端口",
        "td_address": "tcp://交易服务器IP:端口",
        "product_info": "",
        "auth_code": ""
    },
    # 可以添加多个账号
    {
        "account_name": "账号2",
        "gateway_type": "CTP",
        "username": "...",
        "password": "...",
        ...
    }
]
```

3. 运行脚本添加账号：
```powershell
conda activate vnpyenv
python add_accounts.py
```

**方法B：直接编辑JSON文件**

1. 配置文件位置：`C:\Users\YourName\.vntrader\connect_ctp.json`
2. 编辑该文件，填入您的账号信息：

```json
{
    "用户名": "您的真实用户名",
    "密码": "您的真实密码",
    "经纪商代码": "您的经纪商代码",
    "交易服务器": "tcp://交易服务器IP:端口",
    "行情服务器": "tcp://行情服务器IP:端口",
    "产品名称": "",
    "授权编码": "",
    "产品信息": ""
}
```

### 步骤2：安装CTP Gateway模块（如果未安装）

```powershell
conda activate vnpyenv
pip install vnpy_ctp
```

### 步骤3：运行自动加载脚本

```powershell
conda activate vnpyenv
python run_with_accounts.py
```

脚本会自动：
- ✅ 检测并加载所有账号配置
- ✅ 自动注册CTP Gateway
- ✅ 自动连接所有配置的账号
- ✅ 启动VeighNa Trader界面

## 三、多个账号配置

如果您有多个账号，可以为每个账号创建独立的配置文件：

- `connect_ctp_账号1.json`
- `connect_ctp_账号2.json`
- `connect_ctp_账号3.json`

脚本会自动检测并加载所有配置文件。

## 四、验证配置

运行测试脚本查看已配置的账号：

```powershell
conda activate vnpyenv
python test_account_config.py
```

## 五、从C#代码转换账号信息

如果您的C#代码中有账号配置，参考以下对应关系：

| C# 变量 | VeighNa 字段 | 说明 |
|---------|-------------|------|
| `username` | `用户名` | 交易账号 |
| `password` | `密码` | 交易密码 |
| `brokerId` | `经纪商代码` | 期货公司代码 |
| `tdAddress` | `交易服务器` | 交易服务器地址 |
| `mdAddress` | `行情服务器` | 行情服务器地址 |

**示例转换：**

```csharp
// C# 代码
string username = "123456";
string password = "mypassword";
string brokerId = "9999";
string tdAddress = "tcp://180.168.146.187:10130";
string mdAddress = "tcp://180.168.146.187:10131";
```

转换为Python配置：

```python
{
    "用户名": "123456",
    "密码": "mypassword",
    "经纪商代码": "9999",
    "交易服务器": "tcp://180.168.146.187:10130",
    "行情服务器": "tcp://180.168.146.187:10131",
    "产品名称": "",
    "授权编码": "",
    "产品信息": ""
}
```

## 六、注意事项

1. **密码安全**：配置文件中的密码是明文存储，请妥善保管
2. **服务器地址格式**：必须是 `tcp://IP:端口` 格式
3. **多个账号**：每个账号需要独立的配置文件或Gateway实例
4. **模块安装**：确保已安装 `vnpy_ctp` 模块才能自动连接

## 七、故障排除

### 问题1：提示"未找到账号配置文件"
- 检查配置文件是否在 `.vntrader` 文件夹中
- 运行 `python test_account_config.py` 查看配置位置

### 问题2：提示"vnpy_ctp未安装"
- 运行 `pip install vnpy_ctp` 安装模块

### 问题3：连接失败
- 检查账号信息是否正确
- 检查网络连接
- 查看VeighNa Trader的日志窗口获取详细错误信息

## 八、当前状态

✅ 配置文件系统已就绪
✅ 自动加载脚本已创建
✅ 测试脚本验证通过

**下一步**：请添加您的真实账号信息，然后运行 `python run_with_accounts.py`

